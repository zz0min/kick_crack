<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 배경색 (흰색) -->
    <stroke
        android:width="2dp"
        android:color="#4CAF50" /> <!-- 테두리 색상 (초록색) -->
    <corners android:radius="8dp" /> <!-- 모서리 둥글기 -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
